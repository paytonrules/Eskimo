/*describe("Vector", function() {
  var Vector = require("../src/vector");

  it("creates a 2d vector", function() {
    var vector = Vector.create2DVector(1, 4);

    vector.x.should.equal(1);
    vector.y.should.equal(4);
  });

  it("has a lengthSquared property", function() {
    var vector = Vector.create2DVector(3, 4);

    vector.lengthSquared.should.equal(25);
  });

  it("returns a new normalized version of the vector", function() {
    var vector = Vector.create2DVector(3, 4);
    var normalizedVector = vector.normalized();

    normalizedVector.x.should.equal(0.6);
    normalizedVector.y.should.equal(0.8);
  });

  it("Returns a scaled version of the vector", function() {
    var vector = Vector.create2DVector(3, 4);
    var scaledVector = vector.scaled(2.0);

    scaledVector.x.should.equal(1.2);
    scaledVector.y.should.equal(1.6);
  });

});*/
